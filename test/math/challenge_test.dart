import 'package:geb/math/challenges.dart';
import 'package:test/test.dart';

main() {
  test('challenges', () {
    // No assertions; just evaluate the "challengeSets" variable to make sure it
    // doesn't crash (this ensures that all the formulas are valid).
    challengeSets;
  });
}